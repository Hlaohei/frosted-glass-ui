/*
 * 文档页面样式修改
 */
* {
    // 切换主题时背景颜色与字体颜色切换
    transition: background 0.6s, background-color 0.6s, color 0.2s;
}

body {
    // 字体覆盖
    font-family: -apple-system, BlinkMacSystemFont, 'Microsoft YaHei', 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Fira Sans', 'Droid Sans', 'Helvetica Neue', STHeiti, SimSun, sans-serif;
}

@media (min-width: $MQNarrow) {
    // 首页主区域高度
    .home .hero {
        height: 35rem;
    }
}

// 页面底部编辑时间与贡献者的间距
.update-time {
    margin-left: 1rem;
}